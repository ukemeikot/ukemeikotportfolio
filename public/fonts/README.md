<!-- REFACTORED -->
# Font Assets

Place the self-hosted font files below in this directory to fully enable the configured typography:

- `ClashDisplay-Variable.woff2`
- `CabinetGrotesk-Variable.woff2`

The application is already wired to reference these files through `@font-face` rules in `src/styles/globals.css`.
